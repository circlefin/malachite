syntax = "proto3";

package test;

import "malachite.common.proto";

message BlockMetadata {
    bytes proof = 1;
    malachite.common.Value value = 2;
}

message Content {
    oneof value {
        malachite.common.TransactionBatch tx_batch = 90;
        BlockMetadata metadata = 91;
    }
}

