syntax = "proto3";

package test;

import "malachite.proto";

message BlockMetadata {
    bytes proof = 1;
    malachite.Value value = 2;
}

// TODO - use oneof
message Content {
    malachite.TransactionBatch tx_batch = 90;
    optional BlockMetadata metadata = 91;
}

