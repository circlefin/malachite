name: MBT
on:
  push:
    branches:
      - main
    paths:
      - Specs/Quint/**
      - Code/**
      - .github/workflows/mbt.yml
  pull_request:
    paths:
      - Specs/Quint/**
      - Code/**
      - .github/workflows/mbt.yml

jobs:
  gen-and-exec-traces:
    name: Generate and execute traces as tests
    runs-on: ubuntu-latest
    env:
      FIXTURES_DIR: Code/itf/tests/fixtures
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: "18"
      - run: npm install -g @informalsystems/quint
      - name: Setup Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
      - name: Install cargo-nextest
        uses: taiki-e/install-action@cargo-nextest
      - name: Build code
        working-directory: Code/itf
        run: cargo nextest run --workspace --all-features --no-run
      - name: Set current elapsed seconds since the Epoch as random seed for Quint
        run: echo "QUINT_SEED=$(date +%s)" >> $GITHUB_ENV
      - name: Run tests from traces (with random seed)
        working-directory: Code/itf
        run: cargo nextest run --workspace --all-features test_itf --no-capture
